# Runs tools/discrimination-check.sh. Submit from the repository root with the
# same flags as cloudbuild.yaml.
steps:
  - id: mutants
    name: maven:3.9.11-eclipse-temurin-17@sha256:e4a7ace3dc0d645ed97f8d9ad0b0d3f0b14fa8d150138f27f116d7105a639b82
    entrypoint: bash
    args: [tools/discrimination-check.sh]

logsBucket: gs://${PROJECT_ID}-build-source/logs
timeout: 2700s
options:
  logging: GCS_ONLY
